{
  "name": "lernskript-mathe",
  "version": "1.0.0",
  "description": "Lernskript Mathematik 2",
  "scripts": {
    "build": "node scripts/build.cjs && node scripts/generate-service-worker.js && node scripts/generate-search-index.js",
    "check-pdfs": "node scripts/check-pdfs.js",
    "watch": "node scripts/watch.js",
    "lint": "eslint .",
    "test": "jest",
    "format": "prettier --write .",
    "prepare": "npm run build"
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.2",
    "babel-jest": "^29.7.0",
    "browser-sync": "^3.0.4",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.2",
    "jsdom": "^26.1.0",
    "prettier": "^3.3.2"
  }
}
