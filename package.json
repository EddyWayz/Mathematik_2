{
  "name": "lernskript-mathe",
  "version": "1.0.0",
  "description": "Lernskript Mathematik 2",
  "scripts": {
    "build": "node scripts/build.js && node scripts/generate-service-worker.js",
    "check-pdfs": "node scripts/check-pdfs.js",
    "watch": "node scripts/watch.js",
    "lint": "eslint .",
    "test": "jest",
    "format": "prettier --write .",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.3.2"
  }
}
